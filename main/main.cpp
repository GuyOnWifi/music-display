#include <Arduino.h> // Include the Arduino core library
#include <Wire.h>    // Include the Wire library for I2C communication
#include <u8g2.h>    // Download the U8g2 library
// Create a U8g2 object, specifying the use of hardware I2C interface,
// and setting the display model to SSD1306 with a resolution of 128x64 pixels
void setup(void) {}
void loop(void) {
  // The program loop, which is empty and does not perform any operations here
}
